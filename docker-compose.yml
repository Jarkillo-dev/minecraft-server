services:
  minecraft:
    build:
      context: .
      args:
        PAPER_VERSION: "1.21.4"
        PAPER_BUILD: "411"
        MC_RAM: "2G"
    ports:
      - "25565:25565"
      - "25575:25575"
    volumes:
      - world-data:/server/world
      - plugins:/server/plugins
      - logs:/server/logs
    environment:
      - MC_RAM=2G
    restart: unless-stopped

volumes:
  world-data:
  plugins:
  logs:
